from typing import Tuple, List, Dict

from misc.enums import CubeEmotionClass

tags_emotions_mapping = List[Tuple[CubeEmotionClass, List[str]]]

vk_group = Dict<str> {
    'id': int,
    'slug': str
}